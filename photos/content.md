it should not be a FLAT RATE. I never said that. If it is undefined, then the shipping for that item should default to 100. Each item in the cart, going through the transaction should have its own shipping cost, then all gets added together for the subtotal

